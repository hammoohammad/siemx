pip install psutil
pip install pywin32

Run this command to execute all the backend files at once: 
start python server.py && start python edr.py && start python dashboard.py && start python monitor.py

start python edr.py && start python monitor.py && start python server.py && start python dashboard.py


